class Solution:
    def mySqrt(self, x: int) -> int:
        L = 1
        R = x

        ans = 0

        while L <= R:
            mid = (L + R) // 2

            if mid * mid <= x:
                ans = mid
                L = mid + 1
            else:
                R = mid - 1
        
        return ans